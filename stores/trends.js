import { defineStore } from 'pinia';

export const useSessionsStore = defineStore("Session", () => {  

    /*
        Store the current graph/visualization data in the current anchor
        Store the graph/visualization data from all previous anchors in the session
        Capture all edits to trends, and store the updated data here
        Provide and receive data from the sessions.js store, anchors.js, and other
        
    */

});